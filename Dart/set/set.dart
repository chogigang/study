//set 기본

//set은 중복값을 허용하지 않는다. 그리고 데이터의 순서가 없다. set으로 데이터를 대입하면
//인덱스가 없습니다. 인덱스로  값 하나하나 접근하지 못합니다.

//set을 선언할때는 list와 다르게 {} 를 사용합니다.

void main(){

  Set<int> myNumSet = {10,20,30,40,100,600};
    Set<String> nation = {'korea','uk','japan','usa','germany','uk'};
 
    print(myNumSet);
    print(nation);
  //var 로 선언하면 여러가지 데이터형의 값을 넣을수 있다.

     var myValue = {'korea','uk','japan',1,2,3};
      print(myValue);


}

