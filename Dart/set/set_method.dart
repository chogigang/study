//set의 메소드들



void mmain(){


Set<int> a={1,2,3,4,5,6,7,8,9};


// add(element): Set에 요소를 추가합니다.
// addAll(iterable): Set에 iterable의 모든 요소를 추가합니다.
// remove(element): Set에서 지정된 요소를 제거합니다.
// contains(element): Set에 지정된 요소가 포함되어 있는지 확인합니다.
// containsAll(iterable): Set에 iterable의 모든 요소가 포함되어 있는지 확인합니다.
// union(otherSet): 현재 Set과 다른 Set의 합집합을 반환합니다.
// intersection(otherSet): 현재 Set과 다른 Set의 교집합을 반환합니다.
// difference(otherSet): 현재 Set에서 다른 Set에 포함되지 않은 요소들을 반환합니다.
// clear(): Set의 모든 요소를 제거합니다.
// length: Set에 포함된 요소의 수를 반환합니다.
// isEmpty: Set이 비어 있는지 여부를 확인합니다.
// isNotEmpty: Set이 비어 있지 않은지 여부를 확인합니다.
// forEach(action): Set의 각 요소에 대해 지정된 동작(action)을 수행합니다.
// toList(): Set의 요소들로 구성된 List를 반환합니다.


}